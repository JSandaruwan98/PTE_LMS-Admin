//for the assign Test page

SELECT t.*, 
       CASE 
           WHEN ta.test_id IS NOT NULL THEN 1
           ELSE 0
       END AS isIn
FROM test t
LEFT JOIN testass ta ON t.test_id = ta.test_id AND ta.batch_id = 20
ORDER BY t.test_id ASC;